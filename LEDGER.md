# Skills Ledger — installed toolchain, change log, cadence gate
# Managed by the vibe-evolve skill. lastChecked gates builder's Phase 0.
# See also ~/.agents/skills/LEARNINGS.md — per-run postmortems appended by
# builder Phase 3/4, read by vibe-evolve step 2 to prioritize real gaps
# over static phase-name guessing.

role: builder pipelinetoolchain
lastChecked: 2026-08-31
checkCadenceDays: 14

installed:
  custom (opencode config, ~/.config/opencode/skills/):
    - builder        # orchestrator: idea -> docs -> ralph loop -> gates
    - vibe-docs      # idea -> docs/ + prd.json (full product, small stories)
    - vibe-build     # runs the ralph loop, supervises, final gates
    - vibe-evolve    # self-improvement loop (this skill)
    - impeccable     # premium UI audit/polish
  ui-ux-pro-max family (~/.opencode/skills/, via skills.paths):
    - ui-ux-pro-max, banner-design, brand, design, design-system, slides, ui-styling
  ecosystem (~/.agents/skills/):
    - agent-browser, find-skills, prompt-architect   # briefs raw ideas (CO-STAR/BROKE) for builder
    - enhance-prompt, prompt-engineering-patterns, momentic-explore-prompt  # candidate prompt skills (picked: prompt-engineering-patterns for AI-story prompts)
    - strix: penetration-testing-with-strix, web-app-penetration-testing, fix-security-vulnerabilities-with-strix  # runtime pentest gate on OmniRoute models
    - design-taste-frontend         # taste-skill
    - frontend-design               # anthropics/skills official design skill (847K+ installs) — cross-check layout/typography/color fundamentals
    - emil-design-eng               # emilkowalski/skills, 246.2K installs, 34.9K-star repo, PASS all 3 audits — Emil Kowalski's (Sonner/Vaul/cmdk creator) design-engineering craft/motion philosophy
    - building-components           # vercel/components.build, 12.0K installs, official Vercel org, PASS all 3 audits — composable/accessible component APIs, design tokens, theming systems
    - shadcn                        # shadcn-ui/ui official skill, 42.1K installs, 122.8K-star repo, PASS/PASS/WARN audits — correct CLI usage + live component-docs lookup, avoids stale/hallucinated shadcn APIs
    - magicuidesign/magicui@magic-ui  # official Magic UI maintainer, 797 installs, 22.1K-star repo, PASS all 3 audits, no account required — premium free Tailwind animated components (marquee, globe, blur-fade, shiny-button)
    - 21st-dev/agent-elements@agent-elements  # official 21st-dev org, 681 installs, PASS/PASS/WARN, no account required (public shadcn-compatible registry) — chat UI components typed around Vercel AI SDK's UIMessage/useChat, targets the mandatory embedded AI chat requirement
    - mattpocock/skills: codebase-design (538K+), domain-modeling (555K+), improve-codebase-architecture (854K+)  # architecture robustness: shared deep-module vocabulary, domain glossary+ADRs, hot-spot refactor pass. 245K-star repo, PASS on Gen Agent Trust Hub/Socket/Snyk audits.
    - wshobson/agents@gdpr-data-handling   # 13.2K installs, 39.4K-star repo, PASS on all 3 security audits — real DSAR/erasure/portability workflows, retention+legal-basis tracking, 72h breach notification, privacy-by-design (PII separation/encryption)
    - getsentry/skills@security-review     # official Sentry, 15.1K installs — dedicated security-focused code review (injection/auth-bypass/SSRF/insecure-deserialization/secrets), run on every final diff alongside security-and-hardening
    - usestrix/strix@owasp-top-10-testing  # 3.1K installs, same repo as the existing Strix pentest skills — structures the runtime pentest around the full OWASP Top 10 categories
    - wshobson/agents@sast-configuration    # 9.3K installs, 39.4K-star repo, PASS all 3 audits — Semgrep/SonarQube/CodeQL SAST setup as a real CI gate, continuous automated static scanning distinct from the manual security-review pass and Strix's runtime DAST
    - wshobson/agents@nodejs-backend-patterns  # 44.3K installs, same trusted 39.4K-star repo, PASS all 3 audits — layered backend architecture, connection pooling, Redis caching, background jobs, real-time/WebSockets: fills the backend-scalability gap left by codebase-design/domain-modeling (general architecture) and api-and-interface-design (API contract shape only)
    - grafana/skills@k6                     # 3.1K installs, official Grafana Labs maintainer (k6 is Grafana's own load-testing tool), PASS/PASS/WARN audits — generates real k6 load-test scripts so shippability is proven with actual p95/p99 latency + error-rate numbers under concurrent load, not a single manual request
    - addyosmani/web-quality-skills@seo         # 43.2K installs, official addyosmani maintainer, MIT license — closes the SEO gap found in the 30-phase SaaS checklist audit (meta tags, sitemap, structured data, robots.txt); same repo's @performance skill also documents Cache-Control/CDN caching headers (partial caching-gap coverage)
    - resend/email-best-practices@email-best-practices  # 7.5K installs, official Resend maintainer — closes the email-deliverability gap (SPF/DKIM/DMARC, transactional templates, retry/failure handling)
    - resend/react-email@react-email            # 8.2K installs, official Resend maintainer — React email template authoring paired with email-best-practices
    - prisma: database-setup, postgres, client-api, cli, compute, upgrade-v7, postgres-setup, driver-adapter, mongodb-upgrade, component-compute
    - vercel-labs: optimize, react-best-practices, react-native-skills, view-transitions, composition-patterns, cli-with-tokens, web-design-guidelines  # builder requires: react-best-practices, composition-patterns, web-design-guidelines, optimize (Phase 3 perf gate), view-transitions (Phase 2 design execution). Excluded: react-native-skills (not applicable, web-only stack), cli-with-tokens (deploy-execution, conflicts with never-push non-negotiable)
    - addyosmani: spec-driven, test-driven, planning-and-task-breakdown, api-and-interface-design, code-review-and-quality, code-simplification, debugging-and-error-recovery, security-and-hardening, observability, ci-cd-and-automation, shipping-and-launch, performance-optimization, using-agent-skills, context-engineering, documentation-and-adrs, constraint-driven-development, doubt-driven-development, git-workflow-and-versioning, spec-driven-development, planning-and-task-breakdown, etc.
    - idea-refine   # rigor backbone for vibe-docs, now a builder-required install (was silently optional before)
    - source-driven-development  # grounds each ralph iteration's stack decisions (Next.js/Prisma/Auth.js) in current official docs, not stale training data
  references:
    - @design-md  -> ~/.agents/design-md/awesome-design-md (DESIGN.md collections)
    - ralph       -> ~/.agents/ralph (loop driver, backend-agnostic: opencode/claude/codex; bundled in skills/vibe-build/ralph/)
    - agency-agents -> GitHub research reference for multi-agent orchestration, delegation, and product-ops workflows; useful for full-stack app build systems that coordinate specialists instead of one monolithic agent.
    - ecc -> GitHub research reference (real repo: affaan-m/ECC) for agent-harness performance optimization - skills, instincts, memory, security, and research-first development discipline for Claude Code/Codex; useful for hardening the ralph runner's own agent-facing config, not for multi-role coordination (corrected 2026-09-07: originally misidentified as a specialist/coordination-agent pattern).
    - Graphify -> GitHub research reference (real repo: Graphify-Labs/graphify) for graph-first codebase/knowledge-graph intelligence - verified as an actual installable CLI+skill (`graphify install`), not just a conceptual pattern; useful for context retrieval, architecture mapping, and knowledge-heavy product flows.
    - skylos -> GitHub research reference (real repo: duriantaco/skylos) for local-first PR scanning - dead code, security bugs, secrets, and quality regressions caught before merge; useful as a pre-merge quality gate, NOT for workspace/app-shell UX (corrected 2026-09-07: originally misidentified as an OS-style orchestration/product-surface pattern).
    - instatic -> GitHub research reference for static/site-generation and editorial publishing patterns; useful for marketing pages, blog/content hubs, and high-quality product storytelling. Confirmed 2026-09-07 via github.com/topics/instatic (real plugin ecosystem: SEO, pagination, video embeds, site search for the Instatic CMS) - description was accurate.
    - ponytail -> GitHub research reference (real repo: DietrichGebert/ponytail, 130K+ stars) for YAGNI/anti-over-engineering agent discipline - "the best code is the code you never wrote"; useful as a standing constraint against unrequested abstraction during the ralph build loop.
    - rtk -> GitHub research reference (real pattern: a token/context-compression plugin family for coding agents, e.g. enixCode/rtk-plugin "transparent token compression for Claude Code via RTK") - confirmed 2026-09-07 NOT a runtime/tooling/production-workflow-control pattern as originally described; useful only as a possible future context-budget optimization for long ralph loops, not wired into any current step.
    - OpenViking -> GitHub research reference for open-ended agentic app/web workflows and autonomous product tooling; useful for moving from app generation to full-stack operation and content-rich product delivery. STILL UNVERIFIED as of 2026-09-07 after 3 separate lookup attempts (GitHub search 429s, Google returned a JS-challenge page with no results) - do not treat this description as confirmed; if it turns out to name something unrelated the "Post-ship operation mode" step in vibe-build should be treated as standing on its own merit, not this attribution.

changeLog:
  - 2026-09-07: confirmed `instatic` via github.com/topics/instatic (real CMS plugin ecosystem: SEO, pagination, video embeds, site search) - original description was accurate, no correction needed. `OpenViking` remains unverified after 3 lookup attempts across GitHub search and Google (rate-limited / JS-challenge blocked each time); flagged as the one entry in this set whose real-world identity is still unconfirmed.
  - 2026-09-07: confirmed `rtk` via a second GitHub search (rate limit cleared) - it is a token/context-compression plugin family for coding agents (multiple repos: "transparent token compression for Claude Code via RTK", 60-90% context reduction tools), definitively NOT a runtime-control/production-workflow pattern. Removed as the cited source for the ralph runner's self-heal contract (already unattributed in SKILL.md from the prior audit pass); no replacement pattern substituted since the self-heal contract needed no external attribution to begin with. `instatic` and `OpenViking` remain UNVERIFIED (GitHub search still rate-limited).
  - 2026-09-07: wired all 8 reference-stack patterns into concrete, functional pipeline steps (previously logged as inert research references only) — none of these are installed skills or runtime dependencies; each contributes one conceptual pattern applied at a specific step: `agency-agents`/`ecc` → specialist delegation mode for `specialistTrack`-tagged stories (vibe-build §4); `Graphify` → generated `PROJECT-GRAPH.md` dependency-map context step feeding every ralph iteration (vibe-build §4); `skylos` → workspace/app-shell navigation-model design step (vibe-docs design ritual step 6); `instatic` → editorial/narrative content-architecture design step (vibe-docs design ritual step 5); `rtk` → runtime self-heal contract, consecutive-backend-failure fail-fast in the ralph runner; `OpenViking` → post-ship `docs/OPERATIONS.md` runbook for resuming the build-operate loop (vibe-build §7); `ponytail` → YAGNI/anti-over-engineering discipline constraint in vibe-build §0 operating rules (corrected 2026-09-07: originally misidentified as a landing-page/narrative-UX repo; the real repo, `DietrichGebert/ponytail`, is an anti-over-engineering agent-discipline skill, not a content/UI pattern).
  - 2026-09-07: expanded the builder reference layer from "premium web only" into a "full-stack product + editorial content" research stack for external targets: `agency-agents`, `ecc`, `Graphify`, `skylos`, `instatic`, `ponytail`, `rtk`, and `OpenViking`. These are treated as evaluation references for real app architecture, multi-agent orchestration, and high-quality content-driven product experiences, not as installed skills or verified dependencies.
  - 2026-09-03: cross-checked the 30-phase "Complete Web & SaaS Vibe Coding Checklist" PDF against the installed toolchain; found and closed 2 of 6 gaps — added addyosmani/web-quality-skills@seo (43.2K installs, official maintainer, MIT) for SEO/meta/structured-data and resend/email-best-practices + resend/react-email (official Resend maintainer) for email deliverability. Caching/CDN partially covered via web-quality-skills' @performance skill. Stripe/payments and dedicated analytics remain uncovered — no verified official skill found on skills.sh (searches returned no real results); documented as manual-implementation-only per the checklist's own prompts. Wired seo/email-best-practices/react-email into builder's Required-skills list and dependency-install source mapping.
  - 2026-09-03: added magicuidesign/magicui@magic-ui (797 installs, official Magic UI maintainer, 22.1K-star repo, PASS all 3 audits) and 21st-dev/agent-elements@agent-elements (681 installs, official 21st-dev org, PASS/PASS/WARN) after deep web research per a daily.dev "top UI libraries with copy-paste components" roundup and a follow-up skills.sh search — both are official maintainers of premium free component registries, need no external account (unlike the previously-rejected 21st-ai/21st-registry CLI skills, which require a paid 21st.dev API key). agent-elements specifically targets the pipeline's mandatory embedded-AI-chat requirement (shadcn-compatible, typed around Vercel AI SDK's UIMessage/useChat). Wired both into vibe-build's section-3 skill-loading table (new UI-polish and AI-chat-interface rows) and builder's Required-skills list + dependency-install source mapping.
  - 2026-09-03: added grafana/skills@k6 (3.1K installs, official Grafana Labs maintainer, PASS/PASS/WARN per skills.sh) and wshobson/agents@nodejs-backend-patterns (44.3K installs, same trusted 39.4K-star repo as sast-configuration/gdpr-data-handling, PASS all 3 audits) after deep web research into "premium backend design/scalability" and "premium load/performance testing" — the pipeline had general architecture skills (codebase-design/domain-modeling/improve-codebase-architecture) and API-contract design (api-and-interface-design) but nothing backend-implementation-specific (connection pooling, caching, background jobs) and nothing that actually RUNS a load test with real latency/error-rate numbers (only Lighthouse/axe for frontend Core Web Vitals existed before). Wired nodejs-backend-patterns into vibe-build's backend-scalability row and k6 into a new load-testing row + final-gate bullet + checklist item requiring a real k6 run with recorded p95/p99 before shippable.
  - 2026-09-03: added wshobson/agents@sast-configuration (9.3K installs, 39.4K-star repo, PASS on Gen Agent Trust Hub/Socket/Snyk per skills.sh) as a builder-required vulnerability-detection skill — deep web research into "premium vulnerability detection/prevention" found the existing pipeline had strong dependency/SCA scanning, secrets scanning, and container scanning as authored CI-gate guidance, plus manual security-review and runtime Strix pentest, but no dedicated SAST (Semgrep/CodeQL) tool setup for continuous automated static scanning on every commit. Wired into vibe-build's security-pass row and added a new SAST CI-gate bullet + checklist item alongside the dependency/secrets-scan gates.
  - 2026-09-03: added shadcn-ui/ui@shadcn (42.1K installs, official shadcn-ui org, 122.8K-star repo, PASS/PASS/WARN audits per skills.sh) after deep web research per a daily.dev "top UI libraries with copy-paste components" roundup (magicui, aceternity-ui, ui-layout, eldoraui, cuicui, shadcn, indie-starter, fancy-components, 21st.dev, preline, tailus, hyperui, kokonutui, bundui, hextaui, originui, justd, etc.) — cross-checked every named library against skills.sh; only the official shadcn/ui skill itself cleared the bar (others were sub-1K installs, unverified individual forks, or required paid accounts, same pattern as the 21st.dev CLI rejected earlier). The rest are documented in vibe-build as manually-browsed free reference sites only. Wired shadcn into the design-execution row — it gives live `npx shadcn@latest docs <component>` lookups so component usage isn't hallucinated from training data.
  - 2026-09-03: added emilkowalski/skills@emil-design-eng (246.2K installs, 34.9K-star repo, PASS all 3 audits per skills.sh) and vercel/components.build@building-components (12.0K installs, official Vercel org, PASS all 3 audits) as builder-required premium UI/design-engineering skills. Deep web research into 21st.dev and similar sites (per explicit user request) found 21st-dev/skill's own CLI skills (21st-ai, 21st-registry) do NOT clear the reputability bar — only 3 GitHub stars, 639-779 installs, Snyk WARN/FAIL, and require a paid 21st.dev account/API key, conflicting with the pipeline's hands-free/no-cost data-tier philosophy — so 21st.dev is documented in vibe-build as a manually-browsed free reference source only, never an installed CLI dependency.
  - 2026-09-03: added getsentry/skills@security-review (15.1K installs, official Sentry maintainer per skills.sh) and usestrix/strix@owasp-top-10-testing (3.1K installs, same repo as the already-installed Strix pentest skills) — deep web research into "premium hacker attack/security" coverage found the existing security-and-hardening + Strix pipeline only ran a generic checklist plus autonomous exploration, with no dedicated security-focused code review pass and no explicit OWASP Top 10 structuring of the pentest run. Wired security-review into vibe-build's final-diff gate (alongside code-review-and-quality/security-and-hardening) and owasp-top-10-testing into the Strix runtime pentest step (section 6b) so the attack run is walked against all 10 categories explicitly.
  - 2026-09-03: added wshobson/agents@gdpr-data-handling (13.2K installs, 39.4K-star repo, PASS on Gen Agent Trust Hub/Socket/Snyk per skills.sh) as a builder-required privacy-engineering skill — deep web research found the current legal-pages step only produced static Privacy Policy text gated on evidence, with no actual DSAR/erasure/consent-audit/retention-enforcement implementation. Wired into vibe-build's legal-pages section (real working DSAR export + erasure flows, consent audit trail, retention enforcement job, PII encryption/separation) with a matching 6c checklist bullet, and into vibe-docs' SECURITY.md production (breach-notification runbook, privacy-by-design).
  - 2026-09-03: added mattpocock/skills@improve-codebase-architecture (854.1K installs), @domain-modeling (555.1K), @codebase-design (538.5K) — all from the same 245K-star repo, PASS on Gen Agent Trust Hub/Socket/Snyk audits per skills.sh — as builder-required architecture-robustness skills; wired into vibe-docs' ARCHITECTURE.md (domain glossary/ADRs + shared deep-module vocabulary) and vibe-build's Phase 3 gate (run improve-codebase-architecture against real git-log hot spots before shipping). Also added explicit fault-tolerance guidance to vibe-build's stack section (retry/backoff, circuit breaker, health/readiness endpoints, graceful SIGTERM shutdown, connection-pool caps) since deep web research found no verified high-install skill dedicated to this narrow niche — built the guidance directly instead of force-fitting an unverified skill.
  - 2026-09-03: added anthropics/skills@frontend-design (847.7K installs, official Anthropic maintainer, verified via skills.sh leaderboard) as a builder-required design skill — a genuine gap found via deep web research; wired into vibe-docs' DESIGN.md ritual (cross-check step) and vibe-build's design-execution reference table alongside ui-ux-pro-max/design-taste-frontend/impeccable.
  - 2026-09-03: closed remaining self-evolution gaps — vibe-evolve gains step 3b (self-patch: additive, auditable edits to builder/vibe-docs/vibe-build/vibe-evolve's own SKILL.md when the same root cause recurs across 2+ LEARNINGS.md entries, never weakening a gate/non-negotiable), step 4b (effectiveness measurement: baseline stall/checklist-fail rate recorded at install time, compared on later runs, rollback/deprioritize if worse or flat across 2+ runs), and a LEARNINGS.md retention cap (rotate to LEARNINGS.archive.md past ~500 entries/2MB, keep last 200 live).
  - 2026-09-03: closed the self-evolution feedback-loop gap — builder Phase 3/4 now appends a compact structured postmortem (stalls/skill-gaps/strix-rounds/checklist-fails/cost) to ~/.agents/skills/LEARNINGS.md on every run (COMPLETE or PARTIAL); vibe-evolve step 2 reads and tallies it before hunting, so real recurring pain (not just static phase-name enumeration) drives what find-skills is asked to search for next.
  - 2026-08-31: added vercel-optimize (Phase 3 performance gate) and vercel-react-view-transitions (Phase 2 design execution) to builder's Required skills; vercel-react-native-skills and vercel-cli-with-tokens/deploy-to-vercel deliberately excluded (not applicable / conflicts with never-push non-negotiable).
  - 2026-08-31: added source-driven-development as builder-required, loaded in Phase 2 alongside vibe-build's fixed stack section — guards against ralph iterations defaulting to stale/outdated API patterns for Next.js/Prisma/Auth.js across a long unattended run.
  - 2026-08-31: builder's Required-skills list now includes spec-driven-development, idea-refine, and planning-and-task-breakdown (previously only conditionally referenced by vibe-docs as "if installed" — promoted to required so Phase 0 guarantees them) plus documentation-and-adrs (grounds docs/DECISIONS.md's record-every-assumption requirement).
  - 2026-08-31: builder now requires + integrates 5 previously-uninstalled-by-builder skills at specific pipeline points: constraint-driven-development (Phase 1, formalizes Non-negotiables into CONSTRAINTS.md), context-engineering (Phase 2, governs OPENCODE.md per-iteration prompt so fresh headless instances don't drift), git-workflow-and-versioning (Phase 2, atomic/Conventional Commits), debugging-and-error-recovery (Phase 2 stall diagnosis, replaces ad hoc heuristics), doubt-driven-development (Phase 3, adversarial pre-ship review). All 5 were already present under ~/.agents/skills/ (no new install needed) but were missing from builder's own Required-skills list and this ledger's explicit record.
  - 2026-08-31: strix pentest gate integrated into builder/vibe-build/vibe-docs (final audit story + Phase 3 gate + provisioning); Strix runs on the same OmniRoute models as opencode (STRIX_LLM=openai/auto, LLM_API_BASE=localhost:20128)
  - 2026-08-31: installed strix skills (penetration/web-app/fix) + prompt skills (enhance-prompt, prompt-engineering-patterns, momentic-explore-prompt)
  - 2026-08-31: audit 3 — ralph loop smoke-tested end-to-end (fresh opencode run implements story, commits, flips passes, COMPLETE); driver gains post-iteration integrity check (exactly one false->true flip); builder persists the prompt-architect brief to BRIEF.md (vibe-docs consumes it as authoritative)
  - 2026-08-31: prompt-architect integrated into builder (Phase 1: idea -> research-oriented brief before vibe-docs)
  - 2026-08-31: pipeline created (builder/vibe-docs/vibe-build, ralph opencode driver, find-skills wired for self-improvement)